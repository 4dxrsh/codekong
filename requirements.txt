# Pinned to versions current as of 2026-07 (verify with `pip install -r`).
# Everything here is free and runs fully locally.
mutmut>=3.2,<4
pytest>=8.2,<9
sentence-transformers>=3.0,<6
chromadb>=0.5,<2
ollama>=0.4,<1
pandas>=2.2,<3
matplotlib>=3.8,<4
pyyaml>=6.0,<7
python-dotenv>=1.0,<2
