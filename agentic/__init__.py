# Agentic refinement layer: five lightweight components communicating through
# JSON files. A mutant that survives its first generated test triggers exactly
# ONE retry with structured validator feedback — never an unbounded loop.
